dist/
build/
*.signed.shortcut
*.plist
*_processed.cherri
__pycache__/
.pytest_cache/
tmp/

# Compiled shortcut artifacts belong in dist/; keep shortcuts/ source-only.
shortcuts/*.shortcut

# Local personal credentials for local (non-production) builds. NEVER commit.
# (.env.local.example is the committed template and is intentionally tracked.)
.env.local
env.local

# Internal research, planning, and copyrighted references (do not publish)
docs/research-papers/
docs/sagemaker-research/
docs/on-device-model-research.md
docs/context-engineering-plan.md

# Local IDE / MCP settings
.kiro/settings/

# Experimental / scratch scripts
scripts/bench_latency.py
scripts/iris_replica.py
scripts/session_sim.py
